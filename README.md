# homebrew-tap

Homebrew tap for [tyto](https://tyto.run), the CLI for the Tyto Compute API.

```bash
brew install bonyai/tap/tyto
```

The formula is regenerated automatically by the `tyto-cli` release workflow on every
release; it should not be edited by hand.
